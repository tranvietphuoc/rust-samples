// submodules integration tests

pub fn setup() {
    // setup code specific to your library's test would fo here
}

// then run tests again
//
// to avoid having common appear in the test output, instead of creating tests/common.rs, we'll
// create tests/common/mod.rs. this is and alternate naming convention that Rust also understands.
// naming the file this way tell Rust not to treat the common module as an integration test file.
//
